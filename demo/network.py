from rl.src.common import ConvLayer

"""
CONV_LAYERS = [
    ConvLayer(
        filters=32,
        kernel_size=2,
        strides=2,
        activation="relu",
        padding="same",
    ),
    ConvLayer(
        filters=32,
        kernel_size=2,
        strides=2,
        activation="relu",
        padding="same",
    ),
    ConvLayer(
        filters=32,
        kernel_size=2,
        strides=2,
        activation="relu",
        padding="same",
    ),
]
"""
HIDDEN_LAYERS = [512, 256, 128, 64, 32]
CONV_LAYERS = []
